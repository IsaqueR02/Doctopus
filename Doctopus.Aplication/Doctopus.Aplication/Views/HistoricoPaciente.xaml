<Window x:Class="Doctopus.Aplication.Views.HistoricoPaciente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Doctopus.Aplication.Views"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Histórico e Evolução" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryHueMidBrush" Color="#FF448AFF"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="lblTitulo" Text="Histórico Clínico: NOME DO PACIENTE" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                       Foreground="{StaticResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Acompanhamento visual da evolução terapêutica" FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" Margin="0,0,0,20" Padding="15" UniformCornerRadius="10">
            <Grid>
                <TextBlock Text="📈 Curva de Aprendizado / Participação" FontWeight="Bold" VerticalAlignment="Top"/>

                <lvc:CartesianChart x:Name="GraficoEvolucao" Margin="0,20,0,0" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Nota (1 a 5)" MinValue="0" MaxValue="6"/>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis x:Name="EixoXDatas" Title="Data da Sessão" Labels="{Binding DatasGrafico}"/>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </Grid>
        </materialDesign:Card>
        <materialDesign:Card Grid.Row="2" VerticalAlignment="Stretch" Padding="0">
            <DataGrid x:Name="GridHistorico" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="All">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataAtendimento, StringFormat=dd/MM/yyyy}" Width="100"/>

                    <DataGridTextColumn Header="Profissional" Binding="{Binding Profissional.NomeCompleto}" Width="150" FontWeight="Bold"/>

                    <DataGridTextColumn Header="Especialidade" Binding="{Binding Profissional.Especialidade}" Width="120"/>

                    <DataGridTextColumn Header="Descrição da Sessão" Binding="{Binding Descricao}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Nota" Binding="{Binding NotaParticipacao}" Width="50"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

    </Grid>
</Window>
